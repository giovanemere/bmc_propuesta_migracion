{
  "version": "1.0.0",
  "mcpServers": {
    "bmc-context": {
      "command": "python3",
      "args": ["/home/giovanemere/Migracion/mcp-server.py"],
      "env": {},
      "description": "BMC System Context Provider for AWS Migration v1.0.0"
    }
  },
  "tools": {
    "get_system_context": {
      "description": "Get BMC system context information",
      "parameters": {
        "component": {
          "type": "string",
          "description": "Specific component to get context for (optional)"
        }
      }
    },
    "get_architecture_definitions": {
      "description": "Get initial architecture definitions for BMC system"
    },
    "get_development_definitions": {
      "description": "Get development framework and component definitions"
    },
    "get_database_definitions": {
      "description": "Get database architecture and migration definitions"
    }
  }
}
